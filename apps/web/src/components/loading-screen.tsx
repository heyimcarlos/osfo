/** Stable route pending surface. */
export function LoadingScreen() {
  return (
    <main className="grid min-h-dvh place-items-center bg-background text-sm text-muted-foreground">
      Loading Osfo...
    </main>
  );
}
