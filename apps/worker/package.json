{
  "name": "@osfo/worker",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": {
    "./env": "./src/env.ts"
  },
  "scripts": {
    "build": "wrangler deploy --dry-run --outdir dist",
    "dev": "bun --cwd ../..  alchemy dev --stage development --env-file apps/worker/.env",
    "test": "vitest run --config vitest.config.ts && vitest run --config vitest.db.config.ts",
    "typecheck": "wrangler types --check && tsc --noEmit"
  },
  "dependencies": {
    "@effect/platform-browser": "catalog:",
    "@osfo/api": "workspace:*",
    "@osfo/auth": "workspace:*",
    "@osfo/db": "workspace:*",
    "drizzle-orm": "0.45.2",
    "effect": "catalog:",
    "postgres": "3.4.9"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "catalog:",
    "@cloudflare/workers-types": "catalog:",
    "@effect/vitest": "catalog:",
    "@electric-sql/pglite": "0.5.5",
    "typescript": "catalog:",
    "vitest": "catalog:",
    "wrangler": "catalog:"
  }
}
