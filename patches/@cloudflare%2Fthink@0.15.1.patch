diff --git a/dist/think.js b/dist/think.js
--- a/dist/think.js
+++ b/dist/think.js
@@ -964,6 +964,7 @@
 		this._actionLedgerTableEnsured = false;
 		this._actionPendingTableEnsured = false;
 		this._drainingSubmissions = false;
+		this._submissionWaiters = new Map();
 		this._drainingWorkflowNotifications = false;
 		this._submissionAbortControllers = /* @__PURE__ */ new Map();
 		this._programmaticStreamErrors = /* @__PURE__ */ new Map();
@@ -1712,6 +1713,12 @@
 		if (this._activeMessengerContext) return this._activeMessengerContext;
 		return this.messages.at(-1)?.metadata?.messenger;
 	}
+	async followMessengerInput(_acceptance, _context) {
+		return null;
+	}
+	async acceptMessengerInput(_userMessage, _context) {
+		return void 0;
+	}
 	async chatWithMessengerContext(userMessage, callback, context, options) {
 		const previous = this._activeMessengerContext;
 		this._activeMessengerContext = context;
@@ -1904,7 +1911,7 @@
 		};
 	}
 	async _initializeChannels() {
-		if (this.parentPath.length > 0) return;
+		if (this.parentPath.length > 0 || this._messengerRuntime) return;
 		const { channels, messengers: messengerDefs } = resolveChannels(await this.configureChannels(), this.getMessengers());
 		this._channels = channels;
 		if (Object.keys(messengerDefs).length === 0) return;
@@ -5203,6 +5210,37 @@
 			} catch (error) {
 				console.error("[Think] onSubmissionStatus failed", error);
 			}
+		});
+		const waiters = this._submissionWaiters.get(inspection.submissionId);
+		if (waiters && this._isTerminalSubmissionStatus(inspection.status)) {
+			this._submissionWaiters.delete(inspection.submissionId);
+			for (const resolve of waiters) resolve(inspection);
+		}
+	}
+	waitForSubmission(submissionId, options) {
+		this._ensureSubmissionTable();
+		const row = this._readSubmission(submissionId);
+		if (!row) return Promise.reject(new Error("Submission does not exist"));
+		const inspection = this._inspectionFromSubmissionRow(row);
+		if (this._isTerminalSubmissionStatus(inspection.status)) return Promise.resolve(inspection);
+		const signal = options?.signal;
+		if (signal?.aborted) return Promise.reject(signal.reason);
+		return new Promise((resolve, reject) => {
+			const waiters = this._submissionWaiters.get(submissionId) ?? new Set();
+			const finish = (value) => {
+				signal?.removeEventListener("abort", abort);
+				waiters.delete(finish);
+				if (waiters.size === 0) this._submissionWaiters.delete(submissionId);
+				resolve(value);
+			};
+			const abort = () => {
+				waiters.delete(finish);
+				if (waiters.size === 0) this._submissionWaiters.delete(submissionId);
+				reject(signal.reason);
+			};
+			waiters.add(finish);
+			this._submissionWaiters.set(submissionId, waiters);
+			signal?.addEventListener("abort", abort, { once: true });
 		});
 	}
 	onSubmissionStatus(_submission) {}
@@ -7811,6 +7849,7 @@
 		return "scheduled";
 	}
 	async _handleInternalFiberRecovery(ctx) {
+		if (ctx.name === "think:messenger-reply" && !this._messengerRuntime) await this._initializeChannels();
 		return this._chatRecoveryEngine().handleChatFiberRecovery(ctx, {
 			chatFiberPrefix: () => this.constructor.CHAT_FIBER_NAME + ":",
 			unwrapRecoverySnapshot: (fiber) => {
diff --git a/dist/index-s3Pl812H.d.ts b/dist/index-s3Pl812H.d.ts
--- a/dist/index-s3Pl812H.d.ts
+++ b/dist/index-s3Pl812H.d.ts
@@ -1887,6 +1887,8 @@
    */
   configureChannels(): ThinkChannels | Promise<ThinkChannels>;
   getMessengerContext(): MessengerContext | undefined;
+  followMessengerInput(acceptance: unknown, context: MessengerContext): Promise<string | null | { kind: "unavailable" }>;
+  acceptMessengerInput(userMessage: string | UIMessage, context: MessengerContext): Promise<unknown>;
   chatWithMessengerContext(
     userMessage: string | UIMessage,
     callback: StreamCallback,
@@ -2929,6 +2931,7 @@
   private _hasPendingWorkflowNotifications;
   private _drainWorkflowNotifications;
   private _scheduleWorkflowNotificationAlarm;
+  waitForSubmission(submissionId: string, options?: { signal?: AbortSignal }): Promise<ThinkSubmissionInspection>;
   inspectSubmission(
     submissionId: string
   ): Promise<ThinkSubmissionInspection | null>;
@@ -4049,17 +4052,21 @@
 /** Derive a sensible default tag from a reply stage. */
 declare function defaultDeliveryTag(stage: MessengerReplyStage): DeliveryTag;
 interface MessengerReplySnapshot {
+  acceptance?: unknown;
   event: MessengerEvent;
   stage: MessengerReplyStage;
   tag?: DeliveryTag;
   thread?: unknown;
   type: typeof MESSENGER_REPLY_FIBER_NAME;
+  userMessage?: UIMessage;
 }
 declare function messengerReplySnapshot(
   stage: MessengerReplyStage,
   event: MessengerEvent,
   thread?: unknown,
-  tag?: DeliveryTag
+  tag?: DeliveryTag,
+  userMessage?: UIMessage,
+  acceptance?: unknown
 ): MessengerReplySnapshot;
 declare function parseMessengerReplySnapshot(
   snapshot: unknown
@@ -4151,6 +4158,7 @@
   event: MessengerEvent
 ) => MessengerConversationTarget | Promise<MessengerConversationTarget>;
 interface MessengerDefinition {
+  durableAcceptance?: boolean;
   adapter: Adapter;
   adapterName: string;
   capabilities?: MessengerCapabilities;
@@ -4217,6 +4225,8 @@
   ): Promise<void>;
 }
 interface MessengerThinkHost extends MessengerThinkTarget {
+  inspectFiberByKey?(key: string): Promise<{ fiberId: string; name: string; status: string; snapshot?: unknown; createdAt: number } | null>;
+  followMessengerInput?(acceptance: unknown, context: MessengerContext): Promise<string | null | { kind: "unavailable" }>;
   constructor: {
     name: string;
   };
diff --git a/dist/chat-sdk-iuBa-CGM.js b/dist/chat-sdk-iuBa-CGM.js
--- a/dist/chat-sdk-iuBa-CGM.js
+++ b/dist/chat-sdk-iuBa-CGM.js
@@ -245,13 +245,19 @@
 		turnEnded: stage === "completed"
 	};
 }
-function messengerReplySnapshot(stage, event, thread, tag) {
+function messengerReplySnapshot(stage, event, thread, tag, userMessage, acceptance) {
+	const snapshotUserMessage = userMessage === void 0 ? void 0 : {
+		...userMessage,
+		metadata: { messenger: messengerContextFromEvent(event) }
+	};
 	return {
 		event,
 		stage,
 		tag: tag ?? defaultDeliveryTag(stage),
 		thread,
-		type: MESSENGER_REPLY_FIBER_NAME
+		type: MESSENGER_REPLY_FIBER_NAME,
+		userMessage: snapshotUserMessage,
+		acceptance
 	};
 }
 function parseMessengerReplySnapshot(snapshot) {
@@ -263,7 +269,9 @@
 		stage: candidate.stage,
 		tag: candidate.tag ?? defaultDeliveryTag(candidate.stage),
 		thread: candidate.thread,
-		type: MESSENGER_REPLY_FIBER_NAME
+		type: MESSENGER_REPLY_FIBER_NAME,
+		userMessage: candidate.userMessage,
+		acceptance: candidate.acceptance
 	};
 }
 function messengerReplyRecoveryMode(snapshot) {
@@ -287,7 +295,7 @@
 	});
 	const callback = new TextStreamCallback({
 		onVisibleStart: async () => {
-			await checkpoint(messengerReplySnapshot("streaming", snapshotEvent, options.snapshotThread));
+			await checkpoint(messengerReplySnapshot("streaming", snapshotEvent, options.snapshotThread, void 0, options.userMessage, options.acceptance));
 		},
 		visibleSoftLimit: options.policy?.visibleSoftLimit
 	});
@@ -301,7 +309,7 @@
 	try {
 		await options.surface.startTyping?.("Thinking...");
 		const userMessage = options.userMessage ?? toMessengerUserMessage(options.event);
-		const restoreSurface = options.target.bindActiveDeliverySurface?.(options.surface);
+		const restoreSurface = options.bindDeliverySurface === false ? void 0 : options.target.bindActiveDeliverySurface?.(options.surface);
 		try {
 			if (options.target.chatWithMessengerContext) await options.target.chatWithMessengerContext(userMessage, callback, snapshotEvent);
 			else await options.target.chat(userMessage, callback);
@@ -312,7 +320,7 @@
 			callback.close();
 			await post.catch(() => void 0);
 			await options.surface.post(interruptedResponseText).catch(() => void 0);
-			await checkpoint(messengerReplySnapshot("completed", snapshotEvent, options.snapshotThread));
+			await checkpoint(messengerReplySnapshot("completed", snapshotEvent, options.snapshotThread, void 0, options.userMessage, options.acceptance));
 			return;
 		}
 		completedModelTurn = true;
@@ -320,22 +328,22 @@
 		await post;
 		if (!callback.hasText()) await options.surface.post(emptyResponseText);
 		for (const chunk of options.policy?.splitText?.(callback.remainingText()) ?? []) await options.surface.post(chunk);
-		await checkpoint(messengerReplySnapshot("completed", snapshotEvent, options.snapshotThread));
+		await checkpoint(messengerReplySnapshot("completed", snapshotEvent, options.snapshotThread, void 0, options.userMessage, options.acceptance));
 	} catch (error) {
 		callback.fail(error);
 		await post.catch(() => void 0);
 		const failureMode = messengerReplyFailureMode(callback.hasText(), completedModelTurn, options.policy?.isExpectedDeliveryCompletion?.(error, callback));
 		if (failureMode === null) {
-			await checkpoint(messengerReplySnapshot("completed", snapshotEvent, options.snapshotThread));
+			await checkpoint(messengerReplySnapshot("completed", snapshotEvent, options.snapshotThread, void 0, options.userMessage, options.acceptance));
 			return;
 		}
 		if (failureMode === "apologize") {
 			await options.surface.post(interruptedResponseText).catch(() => void 0);
-			await checkpoint(messengerReplySnapshot("completed", snapshotEvent, options.snapshotThread));
+			await checkpoint(messengerReplySnapshot("completed", snapshotEvent, options.snapshotThread, void 0, options.userMessage, options.acceptance));
 			return;
 		}
 		await options.surface.post({ markdown: errorResponseText }).catch(() => void 0);
-		await checkpoint(messengerReplySnapshot("completed", snapshotEvent, options.snapshotThread));
+		await checkpoint(messengerReplySnapshot("completed", snapshotEvent, options.snapshotThread, void 0, options.userMessage, options.acceptance));
 	}
 }
 function toError(error) {
@@ -348,6 +356,22 @@
 	return {
 		...options,
 		adapterName: options.adapterName ?? options.provider
+	};
+}
+function toMessengerBurstUserMessage(definition, event, context) {
+	const current = toMessengerUserMessage(event, definition.channelSpeakerLabel);
+	if (!context?.skipped.length || !event.message) return current;
+	const messages = context.skipped.map((message) => toMessengerUserMessage({
+		...event,
+		message: toMessengerMessage(message)
+	}, definition.channelSpeakerLabel));
+	const text = [...messages, current].flatMap((message) => message.parts).filter((part) => part.type === "text").map((part) => part.text).join("\n\n");
+	return {
+		...current,
+		parts: [{
+			type: "text",
+			text
+		}]
 	};
 }
 var ThinkMessengerRuntime = class {
@@ -355,6 +379,7 @@
 		this.host = host;
 		this.definitionsByAdapterName = /* @__PURE__ */ new Map();
 		this.definitionsById = /* @__PURE__ */ new Map();
+		this.admittingReplies = new Map();
 		this.definitions = normalizeMessengers(definitions);
 		for (const definition of this.definitions) {
 			this.definitionsByAdapterName.set(definition.adapterName, definition);
@@ -381,7 +406,14 @@
 		}
 		const chat = this.chat ?? this.createChat();
 		this.chat = chat;
-		return chat.webhooks[definition.adapterName](request);
+		const pending = [];
+		const response = await chat.webhooks[definition.adapterName](request, { waitUntil: (task) => pending.push(task) });
+		try {
+			await Promise.all(pending);
+			return response;
+		} catch {
+			return new Response("Message acceptance unavailable", { status: 503 });
+		}
 	}
 	async handleFiberRecovery(ctx) {
 		if (ctx.name !== "think:messenger-reply") return false;
@@ -391,13 +423,21 @@
 		if (!definition) throw new Error(`No messenger definition found for recovered messenger ${snapshot.event.messengerId}`);
 		const thread = this.reviveChatObject(snapshot.thread);
 		const mode = messengerReplyRecoveryMode(snapshot);
+		if (mode === "answer" && snapshot.acceptance !== void 0) {
+			await this.resumeAcceptedReply(ctx.id, definition, snapshot, thread);
+			return true;
+		}
 		if (mode === "answer") {
-			await this.answer(definition, snapshot.event, thread, void 0, snapshot.event, async (nextSnapshot) => {
+			await this.answer(definition, snapshot.event, thread, void 0, snapshot.event, snapshot.userMessage, async (nextSnapshot) => {
 				await this.host.resolveFiber(ctx.id, {
 					snapshot: nextSnapshot,
 					status: nextSnapshot.stage === "completed" ? "completed" : "interrupted"
 				});
-			});
+			}, snapshot.acceptance);
+			return true;
+		}
+		if (mode === "apologize" && snapshot.acceptance !== void 0) {
+			await this.host.resolveFiber(ctx.id, { status: "error", error: new Error("Accepted messenger reply delivery outcome is unknown"), snapshot });
 			return true;
 		}
 		if (mode === "apologize") {
@@ -423,16 +463,31 @@
 			}),
 			userName: this.definitions[0]?.userName ?? "think"
 		});
-		chat.onDirectMessage(async (thread, message) => {
+		const processMessage = chat.processMessage.bind(chat);
+		chat.processMessage = (adapter, threadId, messageOrFactory, options) => {
+			const definition = this.definitionsByAdapterName.get(adapter.name);
+			if (!definition?.durableAcceptance) return processMessage(adapter, threadId, messageOrFactory, options);
+			const task = (async () => {
+				const message = typeof messageOrFactory === "function" ? await messageOrFactory() : messageOrFactory;
+				if (message.author.isMe) return;
+				const thread = await chat.thread(threadId);
+				const event = await this.toEvent(definition, { eventKind: "direct-message", message, thread });
+				await this.enqueueReply(definition, event, thread);
+			})();
+			options?.waitUntil?.(task);
+			task.catch(() => {});
+			return task;
+		};
+		chat.onDirectMessage(async (thread, message, _channel, context) => {
 			const definition = this.definitionForThread(thread);
 			if (!definition) return;
 			if (definition.respondTo.includes("direct-message")) await this.enqueueReply(definition, await this.toEvent(definition, {
 				eventKind: "direct-message",
 				message,
 				thread
-			}), thread);
+			}), thread, context);
 		});
-		chat.onNewMention(async (thread, message) => {
+		chat.onNewMention(async (thread, message, context) => {
 			const definition = this.definitionForThread(thread);
 			if (!definition) return;
 			if (definition.subscribeOnMention) await thread.subscribe();
@@ -440,16 +495,16 @@
 				eventKind: "mention",
 				message,
 				thread
-			}), thread);
+			}), thread, context);
 		});
-		chat.onSubscribedMessage(async (thread, message) => {
+		chat.onSubscribedMessage(async (thread, message, context) => {
 			const definition = this.definitionForThread(thread);
 			if (!definition) return;
 			if (definition.respondTo.includes("subscribed-thread") || message.isMention && definition.respondTo.includes("mention")) await this.enqueueReply(definition, await this.toEvent(definition, {
 				eventKind: message.isMention ? "mention" : "subscribed-message",
 				message,
 				thread
-			}), thread);
+			}), thread, context);
 		});
 		chat.onAction(async (event) => {
 			if (!event.thread) return;
@@ -465,33 +520,83 @@
 		});
 		return chat.registerSingleton();
 	}
-	async enqueueReply(definition, event, thread) {
+	async enqueueReply(definition, event, thread, context) {
+		const key = idempotencyKeyForEvent(event);
+		const previous = this.admittingReplies.get(key);
+		if (previous) await previous;
+		const task = this.enqueueReplyOnce(definition, event, thread, context);
+		this.admittingReplies.set(key, task);
+		try { await task; } finally {
+			if (this.admittingReplies.get(key) === task) this.admittingReplies.delete(key);
+		}
+	}
+	async enqueueReplyOnce(definition, event, thread, context) {
 		const snapshotEvent = serializableMessengerEvent(event);
 		const snapshotThread = thread.toJSON();
+		const userMessage = toMessengerBurstUserMessage(definition, event, context);
+		if (definition.durableAcceptance) {
+			const existing = await this.host.inspectFiberByKey(idempotencyKeyForEvent(event));
+			if (existing) {
+				const snapshot = parseMessengerReplySnapshot(existing.snapshot);
+				if (!snapshot) throw new Error("Messenger delivery checkpoint is pending");
+				const original = snapshot.userMessage ?? toMessengerUserMessage(snapshot.event, definition.channelSpeakerLabel);
+				const current = messengerReplySnapshot("accepted", snapshotEvent, snapshotThread, void 0, userMessage).userMessage;
+				if (JSON.stringify(original) !== JSON.stringify(current)) throw new Error("Provider message identity was reused for different input");
+				if (existing.status === "interrupted") await this.handleFiberRecovery({
+					id: existing.fiberId, name: existing.name, snapshot, createdAt: existing.createdAt,
+					recoveryReason: "interrupted"
+				});
+				return;
+			}
+		}
+		const acceptingTarget = definition.durableAcceptance ? await this.resolveTarget(definition, event) : void 0;
+		const acceptance = typeof acceptingTarget?.acceptMessengerInput === "function"
+			? await acceptingTarget.acceptMessengerInput(userMessage, messengerContextFromEvent(event)) : void 0;
+		if (acceptance?.kind === "unavailable") throw new Error("Messenger admission unavailable");
+		const stashed = Promise.withResolvers();
 		const result = await this.host.startFiber(MESSENGER_REPLY_FIBER_NAME, async (fiber) => {
-			fiber.stash(messengerReplySnapshot("accepted", snapshotEvent, snapshotThread));
-			await this.answer(definition, event, thread, fiber, snapshotEvent);
+			fiber.stash(messengerReplySnapshot("accepted", snapshotEvent, snapshotThread, void 0, userMessage, acceptance));
+			stashed.resolve();
+			await this.answer(definition, event, thread, fiber, snapshotEvent, userMessage, void 0, acceptance);
 		}, {
 			idempotencyKey: idempotencyKeyForEvent(event),
+			initialSnapshot: acceptance === void 0 ? void 0 : messengerReplySnapshot("accepted", snapshotEvent, snapshotThread, void 0, userMessage, acceptance),
+			retryOnError: acceptance !== void 0,
 			metadata: {
 				messengerId: event.messengerId,
 				messageId: event.message?.id,
 				provider: event.provider,
 				threadId: event.thread.id
 			},
-			waitForCompletion: true
+			waitForCompletion: acceptance === void 0
 		});
-		if (result.accepted || result.status !== "interrupted") return;
-		const snapshot = parseMessengerReplySnapshot(result.snapshot);
+		if (result.accepted) {
+			if (acceptance !== void 0) await stashed.promise;
+			return;
+		}
+		if (result.status !== "interrupted") {
+			if (acceptance !== void 0 && parseMessengerReplySnapshot(result.snapshot) === null) throw new Error("Messenger delivery checkpoint is pending");
+			return;
+		}
+		const snapshot = parseMessengerReplySnapshot(result.snapshot) ?? (acceptance === void 0 ? null
+			: messengerReplySnapshot("accepted", snapshotEvent, snapshotThread, void 0, userMessage, acceptance));
 		if (!snapshot) return;
 		const mode = messengerReplyRecoveryMode(snapshot);
+		if (mode === "answer" && snapshot.acceptance !== void 0) {
+			await this.resumeAcceptedReply(result.fiberId, definition, snapshot, thread);
+			return;
+		}
 		if (mode === "answer") {
-			await this.answer(definition, snapshot.event, thread, void 0, snapshot.event, async (nextSnapshot) => {
+			await this.answer(definition, snapshot.event, thread, void 0, snapshot.event, snapshot.userMessage, async (nextSnapshot) => {
 				await this.host.resolveFiber(result.fiberId, {
 					snapshot: nextSnapshot,
 					status: nextSnapshot.stage === "completed" ? "completed" : "interrupted"
 				});
-			});
+			}, snapshot.acceptance);
+			return;
+		}
+		if (mode === "apologize" && snapshot.acceptance !== void 0) {
+			await this.host.resolveFiber(result.fiberId, { status: "error", error: new Error("Accepted messenger reply delivery outcome is unknown"), snapshot });
 			return;
 		}
 		if (mode === "apologize") {
@@ -499,9 +604,31 @@
 			await this.host.resolveFiber(result.fiberId, { status: "completed" });
 		}
 	}
-	async answer(definition, event, thread, fiber, snapshotEvent = serializableMessengerEvent(event), checkpoint) {
+	async resumeAcceptedReply(fiberId, definition, snapshot, thread) {
+		const stashed = Promise.withResolvers();
+		const resumed = await this.host.startFiber(MESSENGER_REPLY_FIBER_NAME, async (fiber) => {
+			fiber.stash(snapshot);
+			stashed.resolve();
+			await this.answer(definition, snapshot.event, thread, fiber, snapshot.event, snapshot.userMessage, void 0, snapshot.acceptance);
+		}, { fiberId, resumeInterrupted: true, retryOnError: true });
+		if (resumed.accepted) await stashed.promise;
+	}
+	async answer(definition, event, thread, fiber, snapshotEvent = serializableMessengerEvent(event), userMessage = toMessengerUserMessage(event, definition.channelSpeakerLabel), checkpoint, acceptance) {
+		if (acceptance !== void 0) {
+			const reply = acceptance.kind === "policy" ? acceptance.text
+				: await this.host.followMessengerInput(acceptance, messengerContextFromEvent(snapshotEvent));
+			if (reply?.kind === "unavailable") throw new Error("Accepted messenger reply is temporarily unavailable");
+			const save = checkpoint ?? ((snapshot) => fiber.stash(snapshot));
+			if (reply !== null) {
+				await save(messengerReplySnapshot("streaming", snapshotEvent, thread.toJSON(), void 0, userMessage, acceptance));
+				await thread.post(reply);
+			}
+			await save(messengerReplySnapshot("completed", snapshotEvent, thread.toJSON(), void 0, userMessage, acceptance));
+			return;
+		}
 		const target = await this.resolveTarget(definition, event);
 		await deliverMessengerReply({
+			bindDeliverySurface: target === this.host,
 			event,
 			checkpoint,
 			fiber,
@@ -510,7 +637,8 @@
 			snapshotThread: thread.toJSON(),
 			surface: thread,
 			target,
-			userMessage: toMessengerUserMessage(event, definition.channelSpeakerLabel)
+			userMessage,
+			acceptance
 		});
 	}
 	/**
