--- a/dist/appserver-connector-E223PO3C.js
+++ b/dist/appserver-connector-E223PO3C.js
@@ -622,6 +622,7 @@
     this.#sendDownstream({ jsonrpc: "2.0", method: "initialized" });
     const started = await this.#request("thread/start", {
       sessionStartSource: "startup",
+      ...(this.#config.server === "cua_repl" ? { ephemeral: true } : {}),
       approvalPolicy: "on-request"
     });
     if (!started.ok) {
@@ -761,11 +762,18 @@
     const program = this.#surfaceProgram(name, args);
     if (program === "unknown-tool") return void 0;
     if (program === void 0) {
+      if (this.#config.server === "cua_repl" && name !== "js" && name !== "js_reset") return void 0;
       return {
         threadId: this.#threadId,
         server: this.#config.server,
         tool: name,
-        arguments: args ?? {}
+        arguments: args ?? {},
+        ...(this.#config.server === "cua_repl" ? {
+          _meta: { "x-codex-turn-metadata": {
+            session_id: this.#threadId,
+            turn_id: crypto.randomUUID()
+          } }
+        } : {})
       };
     }
     return {
--- a/dist/chunk-DXHPM4QC.js
+++ b/dist/chunk-DXHPM4QC.js
@@ -1024,13 +1024,13 @@
   if (failures.length === 0) return true;
   return failures.some((failure) => isDeadConnectionFailure(failure.error));
 };
-var createMcpConnectionPool = () => {
+var createMcpConnectionPool = (preserveConnectionsUntilClose = false) => {
   const idle = /* @__PURE__ */ new Map();
   const sweepExpired = Effect2.suspend(() => {
     const now = Date.now();
     const expired = [];
     for (const [key, entry] of idle) {
-      if (now - entry.idleSince < IDLE_TTL_MS) continue;
+      if (preserveConnectionsUntilClose || now - entry.idleSince < IDLE_TTL_MS) continue;
       idle.delete(key);
       expired.push(entry.connection);
     }
@@ -1046,7 +1046,7 @@
       const entry = idle.get(key);
       if (!entry) return { connection: void 0, expired: void 0 };
       idle.delete(key);
-      if (Date.now() - entry.idleSince >= IDLE_TTL_MS) {
+      if (!preserveConnectionsUntilClose && Date.now() - entry.idleSince >= IDLE_TTL_MS) {
         return { connection: void 0, expired: entry.connection };
       }
       return { connection: entry.connection, expired: void 0 };
@@ -2256,7 +2256,7 @@
 };
 var mcpPlugin = definePlugin((options) => {
   const allowStdio = options?.dangerouslyAllowStdioMCP ?? false;
-  const connectionPool = createMcpConnectionPool();
+  const connectionPool = createMcpConnectionPool(options?.preserveConnectionsUntilClose);
   const presetEntries = (allowStdio ? mcpPresets : mcpPresets.filter((preset) => !("transport" in preset && preset.transport === "stdio"))).map((preset) => ({
     id: preset.id,
     name: preset.name,
--- a/dist/sdk/plugin.d.ts
+++ b/dist/sdk/plugin.d.ts
@@ -332,6 +332,8 @@
     readonly family?: string;
 };
 export interface McpPluginOptions {
+    /** Retain idle connections until this plugin closes. Failure eviction still applies. */
+    readonly preserveConnectionsUntilClose?: boolean;
     /**
      * Allow configuring stdio-transport MCP servers. Off by default.
      *
--- a/dist/sdk/connection-pool.d.ts
+++ b/dist/sdk/connection-pool.d.ts
@@ -15,4 +15,4 @@
  * it applies to EVERY parked connection, not only the identity being asked for.
  * A pooled session holds the credential it was dialled with, so an identity that
  * is never requested again must still age out. */
-export declare const createMcpConnectionPool: () => McpConnectionPool;
+export declare const createMcpConnectionPool: (preserveConnectionsUntilClose?: boolean) => McpConnectionPool;
